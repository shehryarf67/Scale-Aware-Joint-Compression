# Runtime dependencies for the scale-aware joint compression study.
#
# Deliberately no CUDA-specific pins. Install the torch wheel that matches your
# platform *before* installing this file if you need GPU support, e.g.
#
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#
# Keep this list in step with [project.dependencies] in pyproject.toml.

# Core modelling stack
torch>=2.2
transformers>=4.40
datasets>=2.19
accelerate>=0.30
evaluate>=0.4
safetensors>=0.4
sentencepiece>=0.2

# Numerics and analysis
numpy>=1.26
pandas>=2.2
scipy>=1.12
scikit-learn>=1.4

# Configuration, logging, progress, system introspection
pyyaml>=6.0
tqdm>=4.66
psutil>=5.9

# Figures
matplotlib>=3.8

# Downstream tasks (§4.3). PINNED EXACTLY, unlike the floors above: §4.8 requires task versions
# recorded, and task definitions change between harness releases, so a floor would let a later
# install score a different task and still call it HellaSwag.
lm-eval==0.4.12
